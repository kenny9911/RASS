# JobReqConsultant - AI 招聘顾问

<div align="center">

![Version](https://img.shields.io/badge/version-0.1.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Next.js](https://img.shields.io/badge/Next.js-16.1-black.svg)

**一个基于对话式交互的 AI 招聘顾问系统，通过自然语言对话帮助招聘团队澄清职位需求并生成招聘方案。**

[功能特点](#功能特点) • [架构设计](#架构设计) • [快速开始](#快速开始) • [使用指南](#使用指南) • [开发指南](#开发指南)

</div>

---

## 📋 概述

JobReqConsultant 是 RAAS（Recruitment Automation Agent System）的核心模块之一，专注于通过对话式交互获取和澄清招聘需求。与 JobReqDelivery 的表单驱动方式不同，本模块采用自然语言对话的方式，让用户像与真人招聘顾问交流一样提供需求信息。

### 核心价值

- **自然交互**: 通过对话而非表单获取需求，降低使用门槛
- **智能追问**: AI 主动识别信息缺失并追问，确保需求完整
- **实时响应**: 流式对话体验，即时反馈
- **成本透明**: 实时显示 Token 使用量和 API 调用成本

### 与 JobReqDelivery 的区别

| 特性 | JobReqConsultant | JobReqDelivery |
|------|------------------|----------------|
| 交互方式 | 对话式 | 表单式 |
| 适用场景 | 快速咨询、需求不明确 | 结构化输入、需求较清晰 |
| 技术栈 | Next.js (Server Actions) | React + Express |
| 实时通信 | Server Actions | WebSocket |
| 数据存储 | 本地 JSON 文件 | 内存/可扩展 |

## ✨ 功能特点

### 核心功能

| 功能 | 描述 |
|------|------|
| 💬 **对话式交互** | 自然语言对话，像与真人顾问交流 |
| 🧠 **三代理协作** | 分析师、研究员、招聘官协同工作 |
| 🔄 **智能迭代** | 最多 3 轮问答确保信息完整 |
| 📊 **实时统计** | 显示 Token 使用量和 API 成本 |
| 📝 **历史记录** | 保存所有咨询记录，可随时查看 |
| 🎨 **现代 UI** | 深色主题，渐变效果，流畅动画 |
| 📱 **响应式设计** | 完美适配桌面和移动设备 |

### 用户界面

- **聊天界面**: 仿 ChatGPT 风格的对话 UI
- **Token 统计**: 实时显示输入/输出 Token 和成本
- **历史侧边栏**: 快速切换历史咨询记录
- **结果卡片**: 结构化展示最终分析结果

## 🏗️ 架构设计

### 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    Next.js Application                           │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                  React Components (Client)                   ││
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         ││
│  │  │ChatInterface│  │HistoryList  │  │ ResultsView │         ││
│  │  │  (对话界面)  │  │ (历史记录)  │  │  (结果展示)  │         ││
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         ││
│  │         │                │                │                 ││
│  │         └────────────────┼────────────────┘                 ││
│  │                          │                                  ││
│  └──────────────────────────┼──────────────────────────────────┘│
│                             │ Server Actions                    │
│  ┌──────────────────────────┼──────────────────────────────────┐│
│  │                   Server Components                          ││
│  │  ┌─────────────────────────────────────────────────────────┐││
│  │  │                    actions.ts                            │││
│  │  │         submitConsultation() / getHistory()              │││
│  │  └─────────────────────────┬───────────────────────────────┘││
│  │                            │                                 ││
│  │  ┌─────────────────────────┼───────────────────────────────┐││
│  │  │                   AI Agents                              │││
│  │  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐     │││
│  │  │  │ Requirements │ │    Market    │ │  Recruiter   │     │││
│  │  │  │   Analyzer   │→│  Researcher  │→│  Consultant  │     │││
│  │  │  └──────────────┘ └──────────────┘ └──────────────┘     │││
│  │  └─────────────────────────┬───────────────────────────────┘││
│  │                            │                                 ││
│  │  ┌─────────────────────────┼───────────────────────────────┐││
│  │  │                 Vercel AI SDK                            │││
│  │  │            generateObject() with Zod Schema              │││
│  │  └─────────────────────────┬───────────────────────────────┘││
│  └────────────────────────────┼─────────────────────────────────┘│
└───────────────────────────────┼──────────────────────────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
        ┌──────────┐     ┌──────────┐     ┌──────────┐
        │OpenRouter│     │  Google  │     │ OpenAI   │
        │          │     │   AI     │     │          │
        └──────────┘     └──────────┘     └──────────┘
```

### AI 代理系统

系统采用三代理协作架构：

#### 1. 需求分析师 (Requirements Analyzer)

**职责**: 分析用户提供的职位描述

```typescript
interface AnalyzerOutput {
  jobTitle: string;           // 标准化职位名称
  analysisSummary: string;    // 需求分析摘要
  missingInfo: string[];      // 缺失信息列表
}
```

#### 2. 市场研究员 (Market Researcher)

**职责**: 研究人才市场，构建候选人画像

```typescript
interface ResearchOutput {
  similarPositions: string[];  // 类似职位
  candidatePersona: string;    // 候选人画像
  capabilityProfile: string;   // 能力概览
  domainAnalysis: string;      // 领域深度分析
}
```

#### 3. 招聘顾问 (Recruiter Consultant)

**职责**: 综合评估，决定是否需要更多信息

```typescript
interface RecruiterOutput {
  clarificationQuestions: string[];  // 澄清问题
  isComplete: boolean;               // 是否完成
  candidateProfile?: string;         // 最终画像
  searchKeywords?: string[];         // 搜索关键词
  difficultyAssessment?: string;     // 难度评估
  finalReport?: string;              // 最终报告
}
```

### 对话状态机

```
┌─────────┐    用户提供需求    ┌──────────┐    信息不足    ┌───────────┐
│ INITIAL │ ─────────────────→ │ ANALYZING│ ──────────────→ │ CLARIFYING│
└─────────┘                    └──────────┘                 └───────────┘
                                    │                            │
                               信息充足                      用户回答
                                    │                            │
                                    ▼                            ▼
                              ┌───────────┐              ┌──────────┐
                              │ COMPLETED │ ←────────────│RE-ANALYZE│
                              └───────────┘   完成/达3轮   └──────────┘
```

## 🚀 快速开始

### 环境要求

- Node.js 20+
- npm / yarn / pnpm / bun

### 安装步骤

1. **进入项目目录**
   ```bash
   cd JobReqConsultant
   ```

2. **配置环境变量**

   创建 `.env.local` 文件：
   ```env
   # LLM 提供商 (选择一个)
   # 方式 1: 使用 OpenRouter (推荐)
   OPENROUTER_API_KEY=your_openrouter_api_key
   LLM_MODEL=google/gemini-3-flash-preview

   # 方式 2: 使用 Google AI 直接
   # GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key
   # LLM_MODEL=gemini-2.0-flash

   # 方式 3: 使用 OpenAI
   # OPENAI_API_KEY=your_openai_api_key
   # LLM_MODEL=gpt-4o

   # 成本计算 (可选，用于显示预估成本)
   GEMINI_3_FLASH_INPUT_COST=2.00
   GEMINI_3_FLASH_OUTPUT_COST=12.00
   ```

3. **安装依赖**
   ```bash
   npm install
   # 或
   yarn install
   # 或
   pnpm install
   ```

4. **启动开发服务器**
   ```bash
   npm run dev
   ```

5. **访问应用**

   打开浏览器访问 http://localhost:3000

## 📁 项目结构

```
JobReqConsultant/
├── src/
│   ├── app/
│   │   ├── page.tsx              # 主页面
│   │   ├── layout.tsx            # 根布局
│   │   ├── actions.ts            # Server Actions (核心逻辑)
│   │   ├── globals.css           # 全局样式
│   │   └── favicon.ico
│   │
│   ├── components/
│   │   ├── ChatInterface.tsx     # 聊天界面组件
│   │   ├── HistoryList.tsx       # 历史记录列表
│   │   ├── ResultsView.tsx       # 结果展示组件
│   │   └── ui/                   # shadcn/ui 组件
│   │       ├── avatar.tsx
│   │       ├── badge.tsx
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       ├── dialog.tsx
│   │       ├── form.tsx
│   │       ├── input.tsx
│   │       ├── label.tsx
│   │       ├── scroll-area.tsx
│   │       ├── separator.tsx
│   │       ├── sheet.tsx
│   │       ├── skeleton.tsx
│   │       └── textarea.tsx
│   │
│   ├── lib/
│   │   ├── agents/
│   │   │   ├── requirementsAnalyzer.ts  # 需求分析师
│   │   │   ├── marketResearcher.ts      # 市场研究员
│   │   │   └── recruiter.ts             # 招聘顾问
│   │   ├── llm.ts                # LLM 配置
│   │   ├── storage.ts            # 数据存储
│   │   ├── types.ts              # 类型定义
│   │   └── utils.ts              # 工具函数
│   │
│   └── data/
│       └── history.json          # 历史记录存储
│
├── public/                       # 静态资源
├── components.json               # shadcn/ui 配置
├── next.config.ts                # Next.js 配置
├── package.json
├── postcss.config.mjs
├── tailwind.config.ts
└── tsconfig.json
```

## 📖 使用指南

### 基本使用流程

#### 1. 开始新对话

打开应用后，AI 顾问会主动打招呼：

> "您好，我是您的AI招聘顾问团队。请把您想招聘职位的**主要职责** (Responsibilities) 和 **任职要求** (Qualifications) 发给我，我会为您制定招聘方案。"

#### 2. 提供职位信息

用自然语言描述您的招聘需求，例如：

```
我们需要招一个高级前端工程师。

主要职责：
- 负责公司核心产品的前端架构设计和开发
- 主导技术选型和代码规范制定
- 带领3-5人的前端小组

任职要求：
- 5年以上前端开发经验
- 精通 React/Vue 等主流框架
- 有大型项目架构经验
```

#### 3. 回答澄清问题

AI 可能会追问更多细节：

> "为了更精准地匹配候选人，我需要向您确认以下信息：
> 1. 这个职位的薪资范围是多少？
> 2. 对候选人的学历有要求吗？
> 3. 需要候选人有特定行业背景吗？"

您只需用自然语言回答即可。

#### 4. 获取分析结果

经过 1-3 轮对话后，系统会生成完整的分析报告，包括：

- **候选人画像**: 理想候选人的详细描述
- **搜索关键词**: 5 个精准的人才搜索关键词
- **难度评估**: 招聘难度和建议

### 界面功能

#### Token 统计

界面顶部实时显示：
- **In**: 输入 Token 数量
- **Out**: 输出 Token 数量
- **Total**: 总 Token 数量
- **Cost**: 预估 API 成本

#### 历史记录

点击左上角的历史图标可以：
- 查看所有历史咨询记录
- 点击记录可恢复之前的对话
- 查看历史分析结果

#### 新建对话

点击 "+" 图标开始新的咨询对话。

## 🔧 技术栈

### 框架与运行时

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 16.1 | React 全栈框架 |
| React | 19.2 | UI 库 |
| TypeScript | 5.x | 类型安全 |

### AI 与 LLM

| 技术 | 版本 | 用途 |
|------|------|------|
| Vercel AI SDK | 6.0 | AI 集成框架 |
| @ai-sdk/openai | 3.0 | OpenAI/OpenRouter 支持 |
| @ai-sdk/google | 3.0 | Google AI 支持 |
| Zod | 4.3 | Schema 验证与类型推断 |

### UI 组件

| 技术 | 版本 | 用途 |
|------|------|------|
| Tailwind CSS | 4.x | 样式框架 |
| shadcn/ui | - | UI 组件库 |
| Radix UI | - | 无头组件 |
| Framer Motion | 12.26 | 动画效果 |
| Lucide React | 0.562 | 图标库 |
| React Markdown | 10.1 | Markdown 渲染 |

### 工具库

| 技术 | 版本 | 用途 |
|------|------|------|
| date-fns | 4.1 | 日期处理 |
| uuid | 13.0 | 唯一 ID 生成 |
| class-variance-authority | 0.7 | 样式变体 |
| tailwind-merge | 3.4 | 样式合并 |

## 🔌 API 说明

### Server Actions

JobReqConsultant 使用 Next.js Server Actions 而非传统 REST API：

#### submitConsultation

```typescript
// src/app/actions.ts
export async function submitConsultation(
  prevState: ConsultationState,
  formData: FormData
): Promise<ConsultationState>
```

**参数**:
- `prevState`: 当前对话状态
- `formData`: 包含用户输入的 FormData

**返回**: 更新后的对话状态

#### getHistory

```typescript
export async function getHistory(): Promise<ConsultationSession[]>
```

**返回**: 所有历史咨询记录

### 状态类型

```typescript
interface ConsultationState {
  id?: string;
  step: 'INITIAL' | 'CLARIFYING' | 'COMPLETED';
  iteration: number;
  jobInput: JobRequisitionInput;
  analysis?: AnalyzerOutput;
  research?: ResearchOutput;
  recruiter?: RecruiterOutput;
  history: { question: string; answer: string }[];
  messages: { role: 'user' | 'assistant'; content: string }[];
  usage: {
    inputTokens: number;
    outputTokens: number;
    totalTokens: number;
    estimatedCost: number;
  };
}
```

## 🎨 自定义配置

### 修改 LLM 模型

编辑 `src/lib/llm.ts`:

```typescript
export function getModel() {
  const modelName = process.env.LLM_MODEL || 'google/gemini-3-flash-preview';
  // ...
}
```

### 修改迭代次数

编辑 `src/lib/agents/recruiter.ts` 中的 prompt：

```typescript
当前是第 ${iteration} 轮沟通 (总共 2-3 轮)。
// 修改括号内的数字可以调整最大迭代次数
```

### 修改成本计算

编辑 `src/app/actions.ts`:

```typescript
function calculateCost(input: number, output: number) {
  const inputRate = parseFloat(process.env.GEMINI_3_FLASH_INPUT_COST || '2.00');
  const outputRate = parseFloat(process.env.GEMINI_3_FLASH_OUTPUT_COST || '12.00');
  // 单位：美元/百万 tokens
}
```

## 🚢 部署

### Vercel 部署 (推荐)

1. 将代码推送到 GitHub
2. 在 Vercel 导入项目
3. 配置环境变量
4. 部署

### 自托管部署

```bash
# 构建
npm run build

# 启动生产服务器
npm run start
```

### Docker 部署

```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app

ENV NODE_ENV=production

COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static

EXPOSE 3000

CMD ["node", "server.js"]
```

## 🤝 与 RAAS 系统集成

JobReqConsultant 作为 RAAS 的对话式入口模块：

- **职位需求入口**: 适合快速咨询和初步需求收集
- **与 JobReqDelivery 互补**: 用户可以从对话切换到表单进行更详细的输入
- **输出对接**: 分析结果可推送到招聘工作台和策略设计器

## 📄 许可证

MIT License

---

<div align="center">

**[返回 RAAS 主项目](../README.md)**

</div>
